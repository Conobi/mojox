# mojox-core

Pure model layer for the mojox build system. No I/O, no subprocess, no filesystem access at module level.

Provides frozen data types, manifest parsing, target discovery, policy resolution, build planning, and metadata serialization.
